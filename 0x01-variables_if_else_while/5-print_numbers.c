#include<stdio.h>
/**
 * main - Prints all single digit numbers with base of 10
 * Return: Always 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%i", i);

	putchar('\n');

	return (0);
}
