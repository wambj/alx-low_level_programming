#include <stdio.h>

/**
 * main - prints all numbers from zero
 * with a base of ten
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
